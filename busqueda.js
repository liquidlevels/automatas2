const arreglo1 = [1,2,3,4,5]
const arreglo2 = [0,2,2,7,5,8]
const arreglo3 = [1,1,3,42,1]


const arreglos = [arreglo1, arreglo2, arreglo3]
var arreglo = //1,2 o 3
//debe distinguir entre las opciones, solo debe tomar las otras que no sean el valor que esta en arreglo
//

/*
 *1,3 -> arreglo3 -> -[0,1,4]
 2 -> arreglo2 -> 1,2
 2 -> arreglo3 -> 2
 var arreglo = 1
 * */

//primero debe tomar el valor inicial del primer arreglo y buscar cuantas veces se repite el numero y en que posicion, una vez haya terminado con el primer arreglo, sigue el segundo
//entonces lo mismo, recorre el arreglo en busca del valor n y en que posicion se encuentra, 

let posicion = 0;

do{
    
}while(posicion < arreglo1.length)
